[package]
name = "libstasis"
version = "0.0.1"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 'z'     # Optimizamos para tamaño
lto = true
strip = true
panic = "abort"
